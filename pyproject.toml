[project]
name = "mlops-04-mlflow-production"
version = "0.1.0"
description = "Production MLflow: tracking server, backend store, and model registry"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dvc[s3]>=3.67.1",
    "mlflow>=3.16.0",
    "scikit-learn>=1.9.1",
]
